struct Point {
    x: int;
    y: int;
    def __init__(self: Point, x: int, y: int) {
        self.x = x;
        self.y = y;
    };
}

def main() -> int {
    X: Point = new Point(0, 1);
    return X.y;
}
