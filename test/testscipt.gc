struct Point {
    x: int;
    y: int;
}

struct Circle {
    center: Point;
    radius: float;
}

struct Rectangle {
    top_left: Point;
    bottom_right: Point;
}

def main() -> int {
    origin: Point = Point(0, 0);
    unit_circle: Circle = Circle(Point(0, 0), 1.0);
    rect: Rectangle = Rectangle(Point(0, 0), Point(1, 1));
    return origin.x;
}
