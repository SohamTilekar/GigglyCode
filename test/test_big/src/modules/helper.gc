@generic(T: Any)
struct Box {
    value: T;

    def __init__(self: Box[T], val: T) {
        self.value = val;
    };

    def getValue(self: Box[T]) -> T {
        return self.value;
    };
};

def compute_sum(a: int, b: int) -> int {
    return a + b;
}
