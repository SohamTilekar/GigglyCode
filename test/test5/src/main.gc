def main() -> int {
    arr: raw_array[int] = raw_array(int, 5);
    i: int = 0;
    while (i < 5) {
        arr[i] = i * 10;
        i = i + 1;
    };
    i = 0;
    while (i < 5) {
        printf("Array element %i : %i\n", i, arr[i]);
        i = i + 1;
    };
    return 0;
}
