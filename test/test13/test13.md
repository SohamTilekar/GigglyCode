# Test 13 - Const and Volatile Variables

This test verifies the variable declaration modifiers: `const` and `volatile`.

## Verified Features:
- **`const` variable declaration:** `const x: int = 42;` creates a read-only variable.
- **`volatile` variable declaration:** `volatile y: int = 100;` creates a volatile variable that can be read and written normally.
